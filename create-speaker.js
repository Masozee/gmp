const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { const speaker = await prisma.speaker.create({ data: { firstName: "Jane", lastName: "Smith", email: "jane.smith@example.com", organization: "Research Institute", position: "Senior Researcher", bio: "Specializes in Deep Learning and Neural Networks" } }); console.log("Created speaker:", speaker); } main().catch(console.error).finally(() => prisma.$disconnect());

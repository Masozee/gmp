"use client"

// UserRole enum
export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
  EDITOR = 'EDITOR',
  CONTRIBUTOR = 'CONTRIBUTOR',
}

// UserCategory enum
export enum UserCategory {
  ACADEMIC = 'ACADEMIC',
  PRACTITIONER = 'PRACTITIONER',
  STUDENT = 'STUDENT',
  OTHER = 'OTHER',
} 